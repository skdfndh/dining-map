# 聚餐地图实现：初始设计记录

## 1. 项目目标

构建一个长期可复用的聚餐行程地图模板。组织者在桌面端编辑一次活动，将地点、时间、参与人、交通路线和费用规则整理后导出为 `event.json`；参与者通过手机打开部署在 GitHub Pages 上的展示页，在一张以地图为主体的页面中查看行程、逐站导航、记录个人进度，并在活动后查看 AA 结算结果。

项目保持纯静态：不使用数据库或自建后端，构建产物放到 GitHub Pages 即可访问。

## 2. 使用角色与页面分工

### 2.1 组织者

- 使用桌面端编辑器 `editor.html`。
- 通过简单密码进入编辑器；这只用于防止误入，不构成真正安全认证。
- 创建、编辑、预览活动并导出 `event.json`。
- 活动后重新导入同一份数据，补充费用并再次发布。

### 2.2 参与者

- 使用手机端展示页 `index.html`。
- 以地图为主要界面查看地点、路线、时间、人员及提醒。
- 可跳转高德地图或百度地图进行导航。
- 可选择“我是谁”，快速查看个人消费、垫付和转账信息。
- 可记录自己已经到达的站点；进度只保存在当前浏览器中。

## 3. 发布及活动生命周期

```text
editor.html
  -> 编辑并自动保存本地草稿
  -> 导出 event.json
  -> 手动替换 GitHub Pages 中的 event.json
  -> index.html 读取并展示活动
```

活动具有三个阶段：

1. 活动前：发布路线、时间、安排和参与人。
2. 活动中：参与者导航、逐站浏览并记录到达进度。
3. 活动后：组织者重新导入 JSON，补充或新增费用，重新导出并发布结算。

每次部署只展示一场活动，但代码和数据格式作为长期模板复用。

## 4. 技术与部署边界

- 建议使用 Vite + TypeScript 构建多页面静态应用。
- 页面、配置和活动数据相互分离。
- 高德地图采用 Web 端 JavaScript API，而非 Web 服务 API。
- 项目使用组织者已提供的高德 Web 端 Key 与安全密钥进行开发配置；凭据不在本文档中重复记录。
- 纯静态部署意味着前端凭据和编辑器逻辑无法真正保密。
- 正式上传 GitHub 前需要再次提醒组织者重新生成项目专用凭据并设置 GitHub Pages 域名限制。
- 展示页尽量兼容微信内置浏览器；导航或定位受限时提示用户在系统浏览器打开。

## 5. 活动数据

活动基本信息包括：

- 活动名称：必填。
- 日期：可选。
- 所在城市：可选，但用于提升搜索及公共交通路线精度。
- 一句简介：可选。
- 结算状态：未开始、整理中、已完成。

数据结构必须带版本号，并为活动、参与人、站点、路线段和费用项目使用稳定的内部唯一编号。重新导入、编辑及发布时应保留编号，以维持参与者本地身份和当前站点状态。

## 6. 参与人

- 组织者先建立活动级参与人名单。
- 每一站单独勾选该站参与人，支持中途加入或离开。
- 每一笔费用单独选择分摊人员。
- 对外主要显示姓名。
- 姓名重复时允许添加辨识备注，例如“同事小王”；内部仍使用唯一编号。

## 7. 地点添加与站点详情

地点支持三种添加方式：

1. 通过高德地点名称或地址搜索。
2. 在地图上长按选点。
3. 粘贴高德或百度地图分享链接。

分享链接在纯静态网页中可能因短链跳转或跨域限制解析失败。失败时引导组织者搜索店名或手动选点，不阻止继续编辑。

每次访问都视为独立站点，即使多个站点坐标相同。站点信息包括：

- 地点简称：用于地图标签。
- 地点全名。
- 详细地址。
- 高德 POI ID 和坐标（可获取时保存）。
- 开始时间或模糊时段。
- 结束时间，可留空。
- 本站活动安排。
- 本站参与人。
- 备注或提醒，例如包间楼层、报号方式等。
- 高德和百度导航目标信息。

## 8. 时间模型与行程排序

站点开始时间支持：

- 精确时间。
- 模糊时段：清晨、上午、中午、下午、傍晚、晚上、深夜。
- 时间待定。

结束时间可填写精确时间，也可留空。活动允许跨午夜和跨日，每个站点记录相对活动开始日期的天数。

首次形成队列时：

1. 精确时间地点按时间自动排序。
2. 模糊时间地点按预设时段顺序排序，同一时段保持添加顺序。
3. 时间待定地点进入“待安排行程”区域。
4. 组织者将待定地点手动拖入已排序队列中的任意位置。

初次自动排序完成后尊重组织者手动顺序。修改时间不会突然重排整个行程；编辑器提供显式“按时间重新排序”操作。

若站点时间与上一段交通耗时冲突，编辑器显示警告和预计迟到时长，但允许保存草稿及继续发布。

## 9. 路线与交通

交通方式包括：

- 步行。
- 骑行。
- 驾车。
- 打车，底层使用驾车路线但界面显示为打车。
- 公共交通。
- 自定义。

高德在编辑阶段自动计算真实道路路线、距离和预计耗时，并将路线坐标及结果冻结到 `event.json`。展示页直接读取，不在每次访问时重新计算。

组织者调整站点顺序后，仅重新计算受到影响的相邻路线段。编辑器提供手动“重新计算路线”操作。

自定义交通允许输入交通说明和耗时，使用虚线连接。高德路线计算失败时也降级为简洁虚线，不阻止活动发布。

## 10. 手机展示页交互

展示页不使用常驻的地图加行程列表结构，而是让地图占据主要视野。

未点击时，地点标签直接展示：

- 站点序号。
- 精确开始时间、模糊时段或“待定”。
- 地点简称。

点击地点后从底部打开详情卡片，显示地点全名、地址、时间、安排、参与人、提醒、本站费用状态，以及高德和百度导航按钮。

点击路线后显示交通方式、距离和耗时。地图支持：

- 完整路线模式。
- 逐站浏览模式。
- 上一站和下一站。
- 已到达和撤销到达。
- 更改当前站。
- 查看完整路线后返回逐站浏览。
- 用户拖动或缩放地图后不强制拉回当前站。

当前站判断优先级：

1. 用户手动设置的已到达进度。
2. 根据活动日期和精确时间推测。
3. 第一个尚未到达的地点。
4. 完整路线总览。

只有模糊时段的站点不做精确时间推断。活动不是当天时默认展示完整路线。

已经过的站点降低亮度并混入灰色，但仍可点击；当前站和下一站保持更醒目的暖色。

“我是谁”和个人到达进度保存在参与者浏览器本地。组织者替换更新后的 `event.json` 时，只要内部编号保持不变，本地状态应继续有效。

## 11. 导航

- 默认从参与者当前手机位置导航至选中地点。
- 分别提供高德地图与百度地图入口。
- 优先尝试唤起对应 App，未安装时尝试打开网页地图。
- 微信内置浏览器限制唤起或定位时，显示“在系统浏览器打开”的明确提示。

## 12. 费用项目与分摊方式

费用既可归属于某一站，也可以是活动级全局费用，例如包车、蛋糕或公共用品。

费用项目可以在活动前创建但暂不填写金额，也允许活动后新增。空金额表示“待结算”，不是 0 元。

每笔费用支持：

- 均分。
- 权重分摊。
- 每人自定义金额。
- 部分人固定金额，剩余金额由其他人均分或按权重分配。
- 某人不参与该笔费用。
- 一人或多人分别垫付，并记录每人的实际垫付金额。

金额统一使用人民币并精确到分。分摊产生无法整除的尾差时采用最大余数法，使分摊总额始终严格等于费用金额。

账目规则：

```text
费用总额 = 所有人的分摊金额之和
费用总额 = 所有垫付金额之和
个人净额 = 应承担金额 - 已垫付金额
```

- 个人净额大于 0：需要付款。
- 个人净额小于 0：需要收款。
- 系统在完整明细之外生成尽可能简化的转账方案。

自定义金额未分配完成时显示剩余未分配金额。编辑器允许保存和导出草稿，但不允许标记为结算完成。

## 13. 结算状态与完成条件

- 未开始：费用尚未整理，展示“费用将在活动后公布”。
- 整理中：允许展示已经确认的部分，并明确提示金额仍可能变化。
- 已完成：展示最终个人消费、垫付、净额和转账方案。

只有同时满足以下条件才能标记为已完成：

1. 所有费用均已填写金额。
2. 每笔分摊金额之和等于费用金额。
3. 每笔垫付金额之和等于费用金额。
4. 全部个人净额之和为 0。

展示页费用区域分为：

1. 我的账单：应消费、已垫付、还需支付或应收回。
2. 我的转账：我应转给谁，或谁应转给我。
3. 全部结算：所有人的消费、垫付和最终转账关系。

每笔费用显示所属站点或“全局费用”，并允许展开查看采用的分配方式。

## 14. 编辑器信息架构

编辑器主要面向电脑，采用“行程编排台”而不是放大的手机页面：

```text
顶部：活动信息、自动保存状态、导入、预览、导出
左侧：地点库、人员库、待安排地点
中间：高德地图、搜索与路线
右侧：当前活动/站点/路线/费用编辑面板
底部：可拖动的完整行程队列
```

编辑器能力包括：

- 每次修改自动保存到当前浏览器。
- 下次打开继续编辑草稿。
- 清空草稿。
- 导入旧 `event.json` 继续修改。
- 手机展示效果预览。
- 导出 `event.json`。
- 导出 CSV 结算表。
- 导出前运行数据校验，区分阻断错误与可忽略警告。

编辑器密码摘要保存在独立配置中，不放进活动数据。提供生成新密码摘要的小工具；验证后在当前浏览器保持登录 7 天，并提供退出编辑器操作。

## 15. 视觉方向

整体概念为“一张铺在餐桌上的行程地图”，风格温暖、有聚餐氛围，同时保持移动端信息清晰。

- 主色：番茄红、烤橙色。
- 背景及卡片：暖奶油色。
- 正文：深酱色，避免纯黑。
- 已到达：降低亮度并掺入灰色，可辅以低饱和鼠尾草绿。
- 地点标签：类似餐桌号码牌，突出序号与时间。
- 路线：不同交通方式使用不同颜色，并具有克制的手绘感。
- 详情卡片：像从底部抽出的菜单卡。
- 动效：集中用于聚焦站点、切换路线和打开卡片，不妨碍地图操作。
- 桌面编辑器信息密度较高；手机展示页让地图成为绝对主角。

## 16. 对外复用接口

- `event.json`：其他程序可直接读取的活动数据。
- 带版本号的数据结构规范。
- 共享的 JavaScript/TypeScript 数据读取、校验、路线处理和 AA 计算模块。
- CSV 结算表导出。

## 17. 第一版建议目录

```text
project/
├─ index.html
├─ editor.html
├─ public/
│  └─ event.json
├─ src/
│  ├─ viewer/
│  ├─ editor/
│  ├─ domain/
│  ├─ maps/
│  ├─ settlement/
│  └─ styles/
├─ config/
├─ openspec/
└─ package.json
```

实际目录以后续 OpenSpec 设计与实施为准。

## 18. 当前非目标

- 多人实时协作编辑。
- 服务端数据库。
- 真正安全的用户登录及权限控制。
- 不同参与者之间同步“已到达”状态。
- 在展示页中直接持久化修改活动公共数据。
- 后端隐藏高德 API 安全密钥。

