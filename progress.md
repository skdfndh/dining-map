# 聚餐地图项目进度

最后更新：2026-08-14

## 项目定位

- GitHub 仓库：`https://github.com/skdfndh/dining-map`
- 参与者展示页：`https://skdfndh.github.io/dining-map/`
- 组织者编辑器：`https://skdfndh.github.io/dining-map/editor.html`
- 技术栈：Vite、React、TypeScript、高德 Web 端 JavaScript API、IndexedDB、GitHub Pages。
- 部署形态：纯静态多页面网站，不使用后端和数据库。
- 本地编辑器密码：`dinner`，仅用于防止误入，不属于安全认证。

## 当前完成度

OpenSpec 变更名称为 `build-dining-map`，采用 `spec-driven` 工作流。目前 100 项任务中已完成 96 项，剩余 4 项均为正式发布前或真实设备验收任务。

已实现的主要能力：

- 编辑器与参与者展示页完全分离。
- 新建聚餐、恢复上个活动、IndexedDB 自动保存、JSON 导入导出。
- 顶部“草稿箱”按活动 ID 保留每个活动最新一次自动保存，最多 24 个；可按名称、日期、地区、地点/参与人数和保存时间辨认，并恢复或删除非当前草稿。
- 旧版浏览器中的当前草稿、滚动快照和“上个活动”会兼容进入草稿箱并自动去重；损坏条目会被忽略，不会阻断其他草稿恢复。
- 省、市、可选区县联动选择；优先使用内置省市中心点离线定位，区县可回退到城市中心，空活动和已有地点的活动都会聚焦新地区。
- 新增参与人后，默认姓名会在首次点击姓名栏时自动清空；后续用户输入不会被反复清除。
- 参与人区域支持浏览器本地历史备选：中文按拼音首字母、英文按拉丁首字母排序，可点击“全部”或有效首字母快速筛选并直接添加；同名可用备注区分，当前活动已有的人会明确显示“已添加”。
- 左栏参与人区域默认只显示已选姓名签和“编辑参与人”；完整姓名、备注、历史选择与删除集中在独立聚焦面板，长名单在面板内部滚动，不再被“今日行程”托盘遮挡。
- 历史备选最多保留 200 条，只保存姓名与可选备注，不随 `event.json` 发布；从历史添加时会为当前活动生成新 ID，避免串联不同活动的数据。
- 删除参与人时会同步清理各站参与记录、费用分摊、权重/自定义金额和垫付记录，存在关联数据时先明确确认。
- 高德搜索、地图右键选点、悬停地名后精确右键选择、高德/百度链接解析降级。
- 精确时间、上午/下午等模糊时段、待定时间、结束时间和跨日时间。
- 待安排地点快捷删除；把已有时间的待安排地点按时间顺序一键追加到现有行程。
- 地图地点牌直接显示开始与结束时间，例如 `14:30–16:30`；跨日显示结束日。
- 手机地图逐站浏览、完整路线、已到达/撤销、当前站推断、高德/百度导航。
- 活动级与站点级费用、均分/权重/自定义/固定后分配、多人垫付、个人账单和简化转账。
- 编辑器预览、发布校验、结算 CSV 导出。
- JSON 导出被阻断时会显示置顶醒目提示和活动标签错误角标，自动打开错误所属工作区；活动名称、行政区等直接可修改字段会被强调并获得焦点，校验明细也可逐条点击定位。
- 公开展示改为 `event.enc.json`：AES-256-GCM 加密，PBKDF2-SHA-256 派生密钥，活动名、日期、地点、姓名和费用均不作为明文元数据公开。
- 展示页解锁前只显示暖色邀请卡，不加载地图或活动信息；查看密码仅在浏览器内存中本地解密，不上传、不保存，刷新后重新输入。
- 编辑器将“明文备份”和“加密发布”分开：公开发布密码至少 12 位并需重复确认，明文 JSON 仅允许组织者私下备份和再次导入。

## 最近完成的自主优化

- 地图地点牌不再拼接用户输入的 HTML，名称和时间均用文本节点渲染，避免活动文件造成脚本/标签注入。
- 活动 JSON 增加 5 MB 上限；坐标、时间、路线距离/耗时、权重和金额的有限值与范围校验更严格。
- 本地会话、查看状态和存储失败都采用安全降级；活动更新后会清理已删除的人、站点和重复到达记录。
- 当前站推断改用本地日历日期，正确覆盖跨午夜、重叠精确时段、未来和已结束活动。
- 到达、撤销和路线选中不再擅自把地图拉回全程；只有初次加载、活动坐标变化或显式聚焦才重设视野，并处理尺寸变化和异步竞态。
- 编辑器支持 Alt + 上下方向键调整行程；预览、地点、路线和费用抽屉支持 Escape 关闭、清晰焦点和对话框语义。
- 桌面工具栏、侧栏、底部行程与手机抽屉增加溢出保护，小于 360px 的标题和间距有独立适配。
- Vitest 已从存在已知严重漏洞的版本升级至 3.2.7，`npm audit` 当前为 0 个已知漏洞。
- 历史参与人选择器采用与编辑器一致的暖色纸卡样式，首字母按钮只显示实际存在的分组，同时保留“新建参与人”入口和屏幕阅读器状态反馈。
- 编辑器在初次读取草稿以及新建、恢复、导入、清空活动期间显示不可编辑的安全过渡页，消除异步存储尚未完成时输入被旧状态覆盖的竞态。
- “清空草稿箱”现在明确提示会删除所有本地活动并要求确认；当前正在编辑的草稿不能在列表中误删。
- 省市区定位不再把所有请求固定为区县级别，也不再依赖高德服务白名单才能工作；主动选择地区会优先使用内置近似中心并聚焦，容器尺寸变化也不会把视野拉回旧地点。

## 最近完成的路线修复

此前路线存在两个逻辑问题：新路段只进入 `stale` 状态但不自动计算；骑行结果使用高德的 `rides` 字段，而旧解析器只读取 `steps/segments`，导致骑行路线被错误替换为端点直线。

当前行为已经调整为：

- 新形成的相邻路段默认自动计算步行道路路线，无需先点击路线卡或计算按钮。
- 切换为骑行、驾车、打车或公交后自动重新计算相应道路路线。
- `计算中`不绘制误导性的端点直线。
- 只有高德算路失败或交通方式为自定义时才使用虚线端点连接。
- 骑行的 `rides` 和公交嵌套段可以正确提取道路折线。
- 少于两个有效折线点的返回结果不会被标记为“已冻结”。
- 旧草稿中曾被误标为成功的端点直线，重新打开或导入后会自动失效并重算。
- 页面实测：切换骑行后无需点击计算按钮，示例路线得到 `2.4 km / 10 分钟`并进入“已冻结”。

## 当前验证结果

- TypeScript 类型检查通过。
- ESLint 无错误或警告。
- 生产构建通过。
- 56 个单元与组件测试通过，新增覆盖活动密文往返与随机化、错误密码、损坏信封、HTML 注入、删除参与人级联、非法坐标/时间、超大文件、损坏存储、跨午夜、状态清理、历史参与人、参与人聚焦面板、发布阻断定位、草稿箱，以及离线省市中心、区县回退、查询级别和已有地点时的地区聚焦优先级。
- 18 个桌面/390px 窄屏端到端场景全部通过；发布阻断后的面板切换、字段聚焦和修复后提示消失，以及草稿箱自动保存、恢复与删除和历史参与人首字母筛选均纳入回归，在线算路成功和高德服务失败后的虚线降级均作为合法且可验证的完成状态。
- 本地浏览器已验证编辑器桌面布局、参与人首次聚焦清空、键盘排序与撤销、预览 Escape 关闭，以及 390px 展示页首屏无横向溢出。
- 本轮应用内浏览器读取本地预览被 URL 安全策略中止，未将视觉检查冒充为完成；桌面与 390px 交互由自动化回归覆盖，真实 iOS/Android、微信内置浏览器和横竖屏仍保留在 OpenSpec 9.2–9.4。
- 本地高德地图实测确认默认步行自动算路和骑行自动重算有效。
- 省市区定位回归确认：正式域名可继续解析行政区中心；当本地开发地址被高德域名白名单拒绝时，请求会在超时后结束并显示可理解提示，不再永久停留在“正在定位”。
- GitHub Pages 隐私版部署完成后应验证：展示页和编辑器返回 HTTP 200，`event.enc.json` 返回 HTTP 200 且不含活动明文，`event.json` 返回 HTTP 404。
- 当前真实活动密文保留完整地点与冻结路线，正确密码解锁后由原有地图界面正常读取。

## 本地使用

在项目根目录运行：

```bash
npm install
npm run dev -- --host 127.0.0.1
```

然后访问：

- 展示页：`http://127.0.0.1:5173/`
- 编辑器：`http://127.0.0.1:5173/editor.html`

高德本地凭据保存在被 Git 忽略的 `.env.local` 中。不要把该文件提交到仓库。

## 完成活动后发布给观看者

1. 在编辑器中确认相邻路线都显示“已冻结”，没有意外的“算路失败”。
2. 点击“预览”，检查手机地图、时间、参与人和费用状态。
3. 点击“加密发布”，设置至少 12 位的独立查看密码并下载 `event.enc.json`。
4. 用下载文件覆盖仓库中的 `public/event.enc.json`；不要把“明文备份”上传到公开仓库。
5. 提交并推送到 `main` 分支。
6. GitHub Actions 的 `Deploy GitHub Pages` 工作流会自动构建并部署。
7. 工作流成功后，把 `https://skdfndh.github.io/dining-map/` 发给参与者，并通过另一条私聊发送查看密码。

编辑器地址不需要发给参与者。以后修改活动或补账时，重新加密导出、替换 `public/event.enc.json` 并推送即可更新同一个观看链接。

## GitHub 与安全状态

- 部署工作流位于 `.github/workflows/deploy-pages.yml`，只在 `main` 分支推送后部署。
- GitHub Pages 已启用并成功完成首次部署，正式地址为 `https://skdfndh.github.io/dining-map/`。
- 当前完整实现已提交并推送到 `main`；首次成功部署包含提交 `49d5fb5`。
- 工作流从 GitHub Actions Secrets 读取 `VITE_AMAP_KEY` 和 `VITE_AMAP_SECURITY_CODE`。
- 上述两个 Secrets 已配置；真实值只保存在本地被忽略的 `.env.local` 和 GitHub Secrets 中，未提交到仓库。
- `.env.local`、构建产物、依赖目录和测试产物均被 Git 忽略。
- 纯静态前端无法真正隐藏 Web JS API 凭据。正式对外使用前，必须在高德控制台把该 Key 的允许域名限制为 `skdfndh.github.io`，不要给它开放任意来源。
- 编辑器简单密码仍只能防误入；活动数据的公开静态保护由独立查看密码和经过认证的加密承担。
- 活动密文仍可被任何人下载并离线猜测密码；拿到正确密码的人可以转存解密内容。这是共享秘密，不是逐用户权限系统，应每次活动使用不同强密码并分开发送链接和密码。
- `editor.html` 已设置不索引，两个入口均使用 `no-referrer`；这些措施只能减少意外暴露，不能把公开静态编辑器变成真正的权限系统。

## 接下来计划

按优先级继续：

1. 在高德控制台配置 `skdfndh.github.io` 域名白名单；如当前凭据曾用于其他场景，正式发布前轮换为此项目专用凭据。
2. 在桌面 Chrome/Edge/Firefox 和真实 iOS/Android 手机完成布局、横竖屏、文字溢出与安全区验收。
3. 完成键盘操作、焦点、对比度、减少动画和地图错误界面验收。
4. 使用 iOS/Android 普通浏览器及微信内置浏览器，填写 `docs/navigation-test-matrix.md` 中的高德/百度导航结果。
5. 所有发布验收完成后，将 OpenSpec `build-dining-map` 变更归档。

## 下次继续时建议先检查

```bash
git status -sb
openspec instructions apply --change build-dining-map --json
npm run typecheck
npm run test
gh run list --repo skdfndh/dining-map --workflow "Deploy GitHub Pages" --limit 5
```

如果后续部署失败，优先检查最近一次 GitHub Actions 日志、Actions Secrets 是否仍存在，以及 Pages Source 是否仍为 GitHub Actions。若网站能打开但地图报错，优先检查高德域名白名单是否允许正式站点。
